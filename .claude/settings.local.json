{
  "permissions": {
    "allow": [
      "Bash(py -c \"import faster_whisper; print\\('faster-whisper OK:', faster_whisper.__version__\\)\")",
      "Bash(py -c \"import torch; print\\('CUDA available:', torch.cuda.is_available\\(\\)\\); print\\('Device:', torch.cuda.get_device_name\\(0\\) if torch.cuda.is_available\\(\\) else 'N/A'\\)\")"
    ]
  }
}
